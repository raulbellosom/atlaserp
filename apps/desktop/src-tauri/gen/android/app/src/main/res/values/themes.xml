<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.runly_erp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.Runly.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#E6EAF0</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.runly_erp</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
