# Atlas Native Host implementation plan

Date: 2026-09-11
Spec: docs/superpowers/specs/2026-09-11-atlas-native-host-design.md
Status: In Progress — user requested implementation; device validation remains mandatory.

## Goal and architecture
Android Tauri hosts /app/ remotely, with a local recovery page, exact-origin IPC, native event queue and centralized frontend bridge. Desktop continues packaging dist.

## File Structure Map
Create: `apps/desktop/native-host/{environments.json,.env.example,shell/*,web-policy.js,smoke-server.mjs,smoke-check.mjs}`, `apps/desktop/scripts/native-host.mjs`, `apps/desktop/src/native/*`, `apps/desktop/src-tauri/src/mobile_host.rs`, `apps/desktop/src-tauri/permissions/native-host.toml`, Android generated project under `apps/desktop/src-tauri/gen/android/`, native tests, and `docs/mobile/ATLAS_NATIVE_HOST.md`.
Modify: root/desktop package.json and pnpm-lock.yaml; Tauri Cargo.toml/Cargo.lock/build.rs/lib.rs/main capability; frontend main.jsx, AppEntry.jsx, serverStore.js, supabase.js, systemNotifications.js, webPush.js; packages/offline runtime predicate; pwa-bootstrap.js; CallsProvider integration if needed; Vite and nginx CSP; .gitignore; docs/TASKS.md. Generated mobile config is ignored. Scope discoveries are recorded here before implementation.

## Task 1 — Configuration and trust policy [implemented]
- [x] Trusted environment manifest and command wrapper; no arbitrary release URL.
- [x] Patched Tauri and platform-specific capability sets.
- [x] Mobile-only CSP header on web delivery; require policy at bootstrap.
Validation: node --test apps/desktop/src/native/__tests__/*.test.js; cargo check.

## Task 2 — Native host [implemented]
- [x] Local shell, configured remote navigation, deadline/ready watchdog.
- [x] Host info, haptics/notification plugins, explicit command ACL.
- [x] Bounded native deep-link queue and ACK; external navigation restrictions.
Validation: cargo fmt --check; cargo test; generated Android config review/build.

## Task 3 — Existing frontend integration [implemented]
- [x] Central bridge and runtime; mobile uses web server configuration.
- [x] No mobile SQL/store or SW registration; preserve web/desktop behavior.
- [x] Ready after React mount, authenticated native-event consumption.
Validation: native node:test suite, frontend build, existing affected tests, React Doctor.

## Task 4 — Android and documentation [implemented]
- [x] Generate Android; microphone/camera declarations and capture with pregranted emulator permissions. Permission-dialog acceptance remains pending.
- [x] Attempt build; record toolchain/device limitations truthfully.
- [x] Document deployment, iOS, push/calls/OAuth roadmap and actual verification.
Validation: Android build and device smoke checklist where available; git diff --check.

## Review
Implementation discovery: shared detection also requires `packages/core/src/native-runtime.js`, core export and offline dependency, plus a Vite alias. Add `src/native/NativeHostDiagnostics.jsx` at authenticated `/app/native-host` to make host info, haptics, notification and media tests executable without console access. Android MainActivity preserves real WebView UA, enables Back and disables popup windows. Android project sources belong in version control; generated Tauri schemas and autogenerated permissions are ignored and recreated by tauri-build. Initial audit: production answered 200 without required CSP and staging did not resolve. The subsequent branding check confirmed production CSP and login in the emulator.

Spec and plan reviewed against user request: no duplicate React, Desktop local preserved, push/calls are future capabilities, iframe boundary explicitly addressed, hardware tests remain separate from unit/build success. No migrations or modifications to pre-existing guest-chat/JWT changes.

## Rollback
### Branding follow-up — 2026-09-11
Replace the Android template icon with the existing `identity/atlas-erp_isotype.svg`, preserving its paths and colors on the official light background. Add `scripts/build-native-brand-assets.mjs` and `brand:native`; use the installed Tauri icon generator for density/adaptive variants, and run it from the native wrapper and existing brand build. Add AndroidX SplashScreen, a launch theme, and the same logo in the bundled recovery shell. Keep intermediate exports in ignored `.tmp/`; retain only resources used by the app. Verify generated output visually, compile Android and inspect the installed launcher/startup in the emulator. This is a correction requested by the user to the existing host, with no auth/IPC changes.

Revert this feature's files only; no database changes. Keep mobile CSP until privileged hosts are retired.

Verification: 2026-09-11 — 9 native Node tests, 22 regressions, 3 Rust tests, Desktop cargo check, Vite build, ESLint, React Doctor 98/100, nginx checks, ARM64/x86_64 debug APK builds and emulator smoke tests. See docs/mobile/ATLAS_NATIVE_HOST.md for evidence, symlink workaround and outstanding authenticated ERP/physical-device acceptance. Implementation is complete; full milestone acceptance is not claimed.

Branding verification: ARM64/x86_64 debug APKs built, launcher/splash/recovery inspected in Android 16 emulator, production login loaded, 9 Node tests and ESLint passed. Intermediate exports and captures remain ignored.
