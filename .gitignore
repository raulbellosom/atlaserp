# Dependencies
node_modules
.pnpm-store

# pnpm workspace artifacts
atlas-erp-starter@*

# Environment — never commit real secrets
.env
.env.local
.env.*.local
# .env.external --- IGNORE --- (may contain real secrets for external services)
.env.external
.env.local

# MCP server config (may contain URLs with tokens)
.mcp.json

# OS
.DS_Store
Thumbs.db

# Build outputs
dist
build
coverage

# Tauri native build
apps/desktop/src-tauri/target

# Prisma generated client
prisma/generated

# Database / logs
*.db
*.db-journal
*.log
pnpm-debug.log*

# Editor
.idea
*.suo
*.ntvs*
*.njsproj
*.sln

# Local task/runtime artifacts
.tmp/
*.tmp
*.pid
task*-*.json
task*-*.headers
task*-*.log
task*-*.pid
task*.tokens.json

# Git worktrees
.worktrees/
